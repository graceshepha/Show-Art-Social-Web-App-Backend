{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Debug database",
            "request": "launch",
            "command": "npm run start",
            "env": {
                "DEBUG": "backend:*"
            },
            "cwd": "${workspaceFolder}"
        }
    ]
}
