{
    "configurations": [
        {
            "type": "node-terminal",
            "name": "Debug backend",
            "request": "launch",
            "command": "npm run start",
            "env": {
                "DEBUG": "backend:*"
            },
            "cwd": "${workspaceFolder}"
        }
    ]
}
